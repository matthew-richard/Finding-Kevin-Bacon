checkstyle!

optimize bst
  --reduce calls to validation functions
 --try labeling with vertices instead of ints
-/-Use HashSet for:
 -- edges, vertices, incoming, outgoing, 

//switch out from overloading equals
